---
import { Header, Projects, SwitchTheme, WorksExperience } from '@/components';
import AppLayout from '@/layouts/app.astro';
---

<AppLayout>
  <main class="p-8 flex flex-col gap-10">
    <SwitchTheme
      className="absolute right-4 top-4 w-[40px]"
      client:only="react"
    />
    <Header client:only="react" />
    <WorksExperience client:only="react" />
    <Projects client:only="react" />
  </main>
</AppLayout>
